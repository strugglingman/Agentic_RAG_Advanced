apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: agentic-rag
  labels:
    app.kubernetes.io/name: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
  ports:
    - name: http
      port: 5001
      targetPort: http
